# Global
spring.application.name=AI Football Predictor
spring.ai.chat.client.enabled=false
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
# Football Data
football-data.base-url=https://api.football-data.org/v4
football-data.competitions=BL1,PL,PD,SA
# Prompts
ai.prompt.prefix=Predict the exact scores for the following matches from the
ai.prompt.suffix=For each match, also provide a confidence score from 1 to 10, where 1 means very uncertain and 10 means very confident. Use current form, injuries, head-to-head history, and any other relevant data. Return only valid JSON with no markdown formatting, no code fences, no explanation outside of it, using this exact format: